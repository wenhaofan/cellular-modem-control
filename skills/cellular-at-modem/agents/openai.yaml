interface:
  display_name: "Cellular AT Modem"
  short_description: "Inspect and control cellular modems with safer AT-command workflows."
  default_prompt: "Use this skill to inspect a local cellular modem, run read-only smoke checks, preview unsafe actions with dry-run, and control SMS, calls, events, or raw AT commands when explicitly requested."
